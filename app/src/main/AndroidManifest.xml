<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epilibre.epilibre">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- For CSV orders download (super_admin) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.StockActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.ChangePasswordActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.UsersEditActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.UsersActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.ProfileActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.HistoricPricesActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.OrderDetails"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.OrdersActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.ProductNewActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.ProductEditActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.ProductsActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.ProductsAdminActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.UsersPendingActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".activities.RegisterActivity"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".activities.LoginActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".activities.LoginActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>